project( 'TuProyecto', 'cpp', default_options: ['cpp_std=c++17'])

# Directorio de salida para librerías y ejecutables
libdir   = 'build/libs'
bindir   = 'build/bin'

# Compilar la librería Tuga
subdir( 'Tuga')

# Compilar el ejecutable Sandbox que usará la librería
subdir( 'Sandbox')
